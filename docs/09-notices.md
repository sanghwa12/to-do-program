---
기능ID: F08
기능명: 공지 (알아둘 것)
마일스톤: 6
상태: 개발중
최종수정: 2026-07-03
---

# F08 · 공지 (알아둘 것)

## 목적
할 일이 아닌 **"알아둬야 하는 정보"**를 할 일과 분리해 관리한다.
예: "외부 강연 - 윤리센터 (7/10)", "코어타임 10~17", "6/27 정전", "폐액통 꽉 채워 버리지 말 것".

## 할 일(Task)과 뭐가 다른가 (이 구분이 이 기능의 핵심)
| | 할 일 | 공지 |
|---|---|---|
| 완료 개념 | 있음 (체크박스) | **없음** — 끝내는 게 아니라 아는 것 |
| 우선순위 | 있음 (점) | 없음 |
| 날짜의 의미 | 마감/기간 | **"그 일이 있는 날"** (강연일, 정전일) |
| 수명 | 완료하면 끝 | 날짜가 지나면 자연히 의미가 옅어짐 → 흐리게 |

## 데이터 모델 (별도 저장 — 할 일과 섞지 않음)
```ts
type Note = {
  id: string;        // 자동 생성
  text: string;      // 내용 (예: "외부 강연 - 윤리센터")
  date?: string;     // 관련 날짜 "YYYY-MM-DD" (없으면 상시 공지)
  createdAt: string; // 자동
};
```
- IndexedDB에 `notes` 테이블 추가 (Dexie version 2 — 기존 tasks 데이터는 그대로 유지됨).
- 할 일에 `kind` 플래그를 넣는 방식은 **안 함**: 모든 할 일 화면(오늘/전체/달력/메모판...)에
  "공지 빼기" 조건이 들어가 실수 여지가 커짐. 분리가 더 단순.

## 사용자 시나리오
- "공지" 판의 전용 입력줄에 `외부 강연 - 윤리센터 7/10` 치고 Enter → 날짜 자동 인식되어 등록.
- 앱을 열면 공지들이 눈에 띄는 곳에 떠 있음. 지난 공지는 흐리게.
- 공지는 체크할 게 없고, 필요 없어지면 삭제.

## 요구사항 (검증 항목)
- [ ] R1. 공지는 할 일과 분리된 `notes` 테이블에 저장 (Dexie version 2, 기존 데이터 무손실).
- [ ] R2. 공지판에 **전용 입력줄**: 텍스트+Enter로 추가, 할 일과 같은 날짜 자동 인식 재사용
      (인식된 날짜를 "그 일이 있는 날"로 저장. 기간 표기면 시작일을 사용).
      **지난 날짜도 그대로 저장** — 할 일과 달리 연도를 내년으로 올리지 않음
      (예: 7/3에 "정전 6/27" 입력 → 올해 6/27. 검증에서 잡힌 오동작 수정, 2026-07-03).
- [ ] R3. 공지판 표시: **오늘 탭 맨 위**, 📢 아이콘 + 무채색(회색 테두리) 판 — 새 색 추가 없음 (D00).
      접기/펼치기 가능 (접으면 "📢 공지 N" 한 줄만). 접힘 상태는 세션 동안만 유지.
- [ ] R4. 정렬: **임박한 공지 → 날짜 없는 공지(최신순) → 지난 공지(흐리게, 맨 아래)**.
      지난 공지가 위를 차지하지 않게 (자동 삭제는 안 함).
- [ ] R5. 공지 수정·삭제 가능. 삭제는 확인 + 실행취소 (할 일과 같은 안전장치).
- [ ] R6. 날짜 있는 공지는 **달력에도 표시** (📢 칩 — 흰 바탕+테두리로 할 일 칩과 구분).
      날짜 칸을 클릭하면 그날 공지도 목록 위에 보인다.
- [ ] R7. 내보내기(.md)에 `## 공지` 섹션으로 포함 (백업 누락 방지).

## 관련 제약
- 서버 없음·IndexedDB·색 최소화 등 기존 원칙 그대로.
- 공지판이 할 일 입력창(주인공)을 밀어내지 않게 — 화면 위계 주의 (D00 원칙 1).

## 관련 파일 (구현하면서 채움)
- (예정) `src/db.js` — notes 테이블 + CRUD
- (예정) `src/components/NoticeBoard.jsx`

## 비고 / 결정 (2026-07-03 사용자 확인)
- **위치**: 오늘 탭 맨 위 + 접기 — "행정 도우미 = 출근해서 게시판 먼저 보는 감각".
  공지가 많아지면 별도 탭 재검토.
- **달력 표시**: 한다.
- 앱의 정체성 메모: 사용자 표현으로는 "할 일 관리라기보다 **나만의 행정 도우미**" —
  이 방향(할 일 + 알아둘 것 + 달력 + 백업)으로 자라는 중.

## 미결정 (나중에)
- 가져오기(붙여넣기)로 공지를 넣는 방법 → 필요해지면 결정.
- 지난 공지 자동 정리(보관함) → 쓰다가 쌓이면 결정.
